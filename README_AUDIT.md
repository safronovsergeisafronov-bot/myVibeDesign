# 🔍 Инструкция по проверке и оптимизации проекта

## Быстрый старт

### Автоматическая проверка (рекомендуется)

```bash
# Запустите скрипт автоматической проверки
./test-audit.sh
```

Скрипт проверит:
- ✅ Сборку проекта
- ✅ Размер бандла
- ✅ Наличие всех компонентов
- ✅ Установленные зависимости
- ✅ Корректность экспортов

---

## Ручная проверка

### 1. Проверка сборки

```bash
npm run build
```

**Ожидается:** Сборка без ошибок, размер основного JS < 1MB

### 2. Запуск dev-сервера

```bash
npm run dev
```

**Откройте:** http://localhost:5173

### 3. Проверка в браузере

#### Старая страница (по умолчанию)

1. **Обзор** - метрики показываются
2. **Цвета** - все палитры видны
3. **Типографика** - все размеры текста
4. **Кнопки** - все варианты работают
5. **Формы** ⚠️ ВАЖНО - должна работать без ошибок
6. **Карточки, Обратная связь, Данные, Иконки, Расширенные** - проверьте каждый таб

#### Новая страница

**Нажмите кнопку "Новые компоненты →" в правом верхнем углу**

1. **Dashboard** - MetricWidget, DataTable, SearchBar, Filters
2. **Формы** - React Hook Form + Zod валидация
3. **Графики** - 4 типа графиков (Line, Bar, Pie, Area)
4. **Образование** - CourseCard, LessonCard, ProfileCards, Timetable, Achievements
5. **Навигация** - ContextMenu (правый клик), TreeView

#### Проверка тёмной темы

**Переключите Dark Mode** (кнопка луны/солнца в header)

**Проверьте:**
- ✅ Весь текст читается (контраст)
- ✅ Границы карточек видны
- ✅ Кнопки видны и кликабельны
- ✅ Формы работают
- ✅ Графики отображаются корректно

---

## Использование AI для проверки

### Вариант 1: Полный аудит с оптимизацией

Скопируйте и отправьте AI:

```
Проверь проект /Users/sergejsafronov/Documents/myVibeDesign используя файл FULL_AUDIT_PROMPT.md

Выполни все 5 частей проверки:
1. Проверка на ошибки (светлая и тёмная тема)
2. Оптимизация производительности
3. Проверка accessibility
4. Финальные проверки
5. Чеклист исправлений

Для каждой найденной проблемы предоставь:
- Описание
- Файл и строку
- Решение с кодом
```

### Вариант 2: Быстрая проверка ошибок

```
Проверь проект myVibeDesign на ошибки:

1. Запусти npm run build и проверь ошибки
2. Проверь консоль браузера на http://localhost:5173
3. Протестируй обе темы (светлую и тёмную)
4. Проверь все табы на обеих страницах
5. Найди проблемы с контрастом, читаемостью, видимостью

Создай список найденных проблем с решениями
```

### Вариант 3: Только оптимизация

```
Оптимизируй производительность проекта myVibeDesign:

1. Проанализируй размер бандла (npm run build)
2. Найди большие импорты (lucide-react, recharts)
3. Примени lazy loading для страниц и графиков
4. Настрой manual chunks в vite.config.js
5. Добавь мemoизацию тяжёлых компонентов

Предоставь конкретные изменения кода
```

---

## Структура файлов проверки

```
/
├── FULL_AUDIT_PROMPT.md    # Полный промпт для AI (все проверки)
├── AUDIT_PROMPT.md          # Простой промпт (только ошибки)
├── test-audit.sh            # Автоматический скрипт проверки
├── README_AUDIT.md          # Этот файл
└── build.log                # Лог сборки (создаётся после ./test-audit.sh)
```

---

## Чеклист перед деплоем

- [ ] `./test-audit.sh` выполнен без ошибок
- [ ] `npm run build` работает
- [ ] Размер основного JS < 1MB
- [ ] Обе темы протестированы
- [ ] Все табы открываются без ошибок
- [ ] Формы работают (старая и новая страницы)
- [ ] Графики отображаются
- [ ] Консоль браузера без красных ошибок
- [ ] Lighthouse score > 80

---

## Известные проблемы и решения

### Проблема 1: "Формы" ломают старую страницу
**Решение:** ✅ Исправлено - используется FormGroup вместо FormField

### Проблема 2: Не видно новых компонентов
**Решение:** ✅ Исправлено - создана NewComponentsPage, кнопка переключения

### Проблема 3: Большой размер бандла
**Решение:**
```bash
# Примените оптимизации из FULL_AUDIT_PROMPT.md раздел 2.3
# Lazy loading + manual chunks
```

### Проблема 4: Изображения CourseCard не загружаются
**Решение:**
```jsx
// Замените unsplash URL на локальные
image: "/images/course-placeholder.jpg"
```

### Проблема 5: Recharts не отображается
**Решение:**
```bash
# Проверьте установку
npm list recharts

# Переустановите если нужно
npm install recharts
```

---

## Поддержка

Если найдены ошибки, которые не удаётся исправить:

1. Проверьте `build.log` для деталей
2. Используйте `FULL_AUDIT_PROMPT.md` с AI
3. Запустите `npm install` для переустановки зависимостей
4. Очистите кэш: `rm -rf node_modules .vite dist && npm install`

---

## Следующие шаги

После успешной проверки:

1. **Деплой на Vercel/Netlify**
   ```bash
   npm run build
   # Загрузите dist/ на хостинг
   ```

2. **Настройка CI/CD**
   - Добавьте `test-audit.sh` в GitHub Actions
   - Автоматическая проверка при каждом commit

3. **Мониторинг производительности**
   - Настройте Web Vitals
   - Используйте Lighthouse CI

---

**Готово! Проект проверен и оптимизирован 🚀**
