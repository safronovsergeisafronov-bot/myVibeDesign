# Рабочие процессы для myVibeDesign

Этот документ описывает стандартные процессы работы с Claude Code для создания продуктов в едином стиле.

---

## 🎯 Основные принципы работы

1. **Всегда проверяй существующие компоненты** - используй `src/components/ui` перед созданием нового
2. **Следуй CLAUDE.md** - все компоненты должны соответствовать дизайн-системе
3. **Используй семантические токены** - `bg-primary` вместо `bg-[#56051B]`
4. **Адаптивность по умолчанию** - все макеты должны работать на мобильных устройствах
5. **Читаемость важнее краткости** - пиши понятный код

---

## 📱 Процесс 1: Создание лендинга/презентации

### Этап 1: Анализ контента
**Цель:** Понять структуру и требования

**Действия:**
1. Прочитай весь контент, который предоставил пользователь
2. Определи основные секции:
   - Hero (главный экран с заголовком)
   - Проблема/Решение
   - Преимущества/Фичи (обычно 3-6)
   - Процесс/Как это работает
   - Social Proof (отзывы, кейсы, статистика)
   - Pricing (если есть)
   - CTA (призыв к действию)
   - Footer

**Вопросы для уточнения:**
- Какая основная цель страницы? (продажи, регистрация, информирование)
- Есть ли конкретные требования к структуре?
- Нужна ли форма захвата лидов?

### Этап 2: Создание структуры страницы
**Цель:** Создать файл с правильной структурой

**Действия:**
1. Создай новый файл в `src/pages/` (например, `SchoolPresentation.jsx`)
2. Импортируй необходимые компоненты из дизайн-системы
3. Создай базовую структуру с секциями:

```jsx
import { Container, Grid, GridItem, Card, Text, Button } from "@/components/ui"

function SchoolPresentation() {
  return (
    <div className="min-h-screen bg-background">
      {/* Hero Section */}
      <section className="py-16 md:py-24">
        <Container>
          {/* Контент героя */}
        </Container>
      </section>

      {/* Features Section */}
      <section className="py-16 bg-card">
        <Container>
          {/* Преимущества */}
        </Container>
      </section>

      {/* CTA Section */}
      <section className="py-16">
        <Container>
          {/* Призыв к действию */}
        </Container>
      </section>
    </div>
  )
}
```

### Этап 3: Наполнение контентом
**Цель:** Заполнить секции реальным контентом

**Правила выбора компонентов:**
- **Hero заголовок:** `<Text variant="hero" color="primary">`
- **Hero подзаголовок:** `<Text variant="subhero">`
- **Заголовки секций:** `<Text variant="h2" as="h2">`
- **Описания:** `<Text variant="body">`
- **Карточки преимуществ:** `<Card variant="default">` - белые карточки на светлом фоне
- **Акцентные блоки:** `<Card variant="burgundy">` - для важной информации
- **Кнопки призыва:** `<Button variant="default" size="lg">` - основная бордовая кнопка

### Этап 4: Адаптивность
**Цель:** Убедиться, что макет работает на всех экранах

**Действия:**
1. Используй `Grid` с responsive настройками:
   ```jsx
   <Grid cols={1} md:cols={2} lg:cols={3} gap={6}>
   ```
2. Настрой padding для секций:
   ```jsx
   className="py-12 md:py-16 lg:py-24"
   ```
3. Адаптируй размеры текста:
   ```jsx
   <Text variant="hero" className="text-4xl md:text-6xl lg:text-8xl">
   ```

### Этап 5: Финальная проверка
**Цель:** Убедиться в качестве

**Чек-лист:**
- [ ] Все тексты отображаются корректно
- [ ] Нет жестко закодированных цветов (только токены)
- [ ] Все кнопки работают (или имеют onClick заглушки)
- [ ] Макет адаптивный (проверь на mobile/tablet/desktop)
- [ ] Используется правильная иерархия заголовков (h1 → h2 → h3)
- [ ] Контраст текста достаточен (особенно на бордовом фоне)
- [ ] Все иконки из `lucide-react` импортированы

---

## 🧩 Процесс 2: Создание нового компонента

### Этап 1: Проверка существующих компонентов
**Цель:** Избежать дублирования

**Действия:**
1. Проверь `src/components/ui/index.js` - список всех компонентов
2. Посмотри похожие компоненты - может можно расширить существующий?
3. Проверь CLAUDE.md - может есть готовый паттерн?

### Этап 2: Определение API компонента
**Цель:** Спроектировать интерфейс

**Вопросы:**
- Какие пропсы нужны? (обязательные и опциональные)
- Нужны ли варианты? (variants: default, primary, secondary)
- Нужны ли размеры? (size: sm, default, lg)
- Должен ли компонент быть составным? (Card + CardHeader + CardContent)

### Этап 3: Разработка компонента
**Цель:** Создать компонент согласно дизайн-системе

**Обязательные требования:**
```jsx
import * as React from "react"
import { cn } from "@/lib/utils"
import { cva } from "class-variance-authority" // для вариантов

const componentVariants = cva(
  "базовые классы", // базовые стили
  {
    variants: {
      variant: {
        default: "стили для default",
        primary: "стили для primary",
      },
      size: {
        sm: "маленький размер",
        default: "стандартный размер",
      }
    },
    defaultVariants: {
      variant: "default",
      size: "default",
    }
  }
)

const ComponentName = React.forwardRef(({
  className,
  variant,
  size,
  children,
  ...props
}, ref) => {
  return (
    <div
      ref={ref}
      className={cn(componentVariants({ variant, size }), className)}
      {...props}
    >
      {children}
    </div>
  )
})
ComponentName.displayName = "ComponentName"

export { ComponentName }
```

**Правила стилизации:**
- Используй только CSS переменные из `index.css`
- Шрифты: `font-inter` для заголовков, `font-onest` для текста
- Радиусы: `rounded-lg` (25px), `rounded-md` (23px), `rounded-sm` (21px)
- Отступы: используй шкалу Tailwind (gap-4, p-6, mt-8)
- Цвета: `bg-primary`, `text-foreground`, `border-input`

### Этап 4: Экспорт компонента
**Цель:** Сделать компонент доступным

**Действия:**
1. Добавь экспорт в `src/components/ui/index.js`:
   ```js
   export { ComponentName } from "./component-name"
   ```
2. Убедись, что файл называется в kebab-case: `component-name.jsx`

### Этап 5: Документирование
**Цель:** Объяснить использование

**Добавь в CLAUDE.md:**
```markdown
### ComponentName
Описание компонента и его назначения.

**Варианты:**
- `variant="default"` - описание
- `variant="primary"` - описание

**Использование:**
\`\`\`jsx
<ComponentName variant="default">
  Контент
</ComponentName>
\`\`\`
```

---

## 📊 Процесс 3: Создание дашборда

### Этап 1: Определение структуры данных
**Цель:** Понять, какие данные отображать

**Действия:**
1. Определи основные метрики (KPI)
2. Определи типы визуализаций:
   - Числовые показатели → `MetricWidget`
   - Временные ряды → `LineChart`, `AreaChart`
   - Сравнение категорий → `BarChart`
   - Доли → `PieChart`
   - Табличные данные → `DataTable`

### Этап 2: Создание макета
**Цель:** Распределить элементы на странице

**Рекомендуемая структура:**
```jsx
<Container className="py-8">
  {/* Метрики (4 колонки) */}
  <Grid cols={4} gap={6} className="mb-8">
    <GridItem>
      <MetricWidget title="Заголовок" value="123" />
    </GridItem>
    {/* ... еще метрики */}
  </Grid>

  {/* Графики (2 колонки) */}
  <Grid cols={2} gap={6} className="mb-8">
    <GridItem>
      <Card>
        <CardHeader>
          <CardTitle>График 1</CardTitle>
        </CardHeader>
        <CardContent>
          <LineChart data={data} />
        </CardContent>
      </Card>
    </GridItem>
    {/* ... еще графики */}
  </Grid>

  {/* Таблица (полная ширина) */}
  <Card>
    <CardHeader>
      <CardTitle>Данные</CardTitle>
    </CardHeader>
    <CardContent>
      <DataTable columns={columns} data={data} />
    </CardContent>
  </Card>
</Container>
```

### Этап 3: Добавление интерактивности
**Цель:** Фильтры, поиск, сортировка

**Компоненты:**
- `SearchBar` - поиск по таблице
- `FilterPanel` + `FilterCheckbox` / `FilterSelect` - фильтрация
- `DatePicker` / `DateRangePicker` - выбор периода
- `Sheet` или `Drawer` - боковая панель с фильтрами

---

## 🎨 Процесс 4: Кастомизация существующего компонента

### Когда НЕ нужно создавать новый компонент:
- Нужен другой цвет → используй пропс `variant` или `className`
- Нужен другой размер → используй пропс `size`
- Нужен другой стиль текста → используй `className` с утилитами Tailwind

### Когда НУЖНО создавать новый компонент:
- Принципиально другая структура
- Новое поведение (логика)
- Будет использоваться в 3+ местах

**Пример кастомизации через className:**
```jsx
// ✅ Правильно - используем существующий компонент
<Card className="border-2 border-primary shadow-xl">
  <CardContent className="p-8">
    Кастомный стиль
  </CardContent>
</Card>

// ❌ Неправильно - создаем новый компонент без необходимости
<CustomCard>...</CustomCard>
```

---

## 🔄 Процесс 5: Рефакторинг страницы

### Когда рефакторить:
- Код дублируется 3+ раза
- Компонент больше 300 строк
- Сложная вложенность (более 5 уровней)

### Шаги рефакторинга:
1. **Определи повторяющиеся паттерны**
   ```jsx
   // Было (повторяется 5 раз)
   <Card>
     <CardHeader>
       <CardTitle>{title}</CardTitle>
     </CardHeader>
     <CardContent>{content}</CardContent>
   </Card>

   // Стало
   <SimpleCard title={title} content={content} />
   ```

2. **Вынеси данные в константы**
   ```jsx
   // В начале файла
   const features = [
     { icon: Star, title: "Фича 1", description: "..." },
     { icon: Zap, title: "Фича 2", description: "..." },
   ]

   // В компоненте
   {features.map(feature => (
     <FeatureCard key={feature.title} {...feature} />
   ))}
   ```

3. **Раздели на секции**
   ```jsx
   function HeroSection() { ... }
   function FeaturesSection() { ... }
   function CTASection() { ... }

   function MainPage() {
     return (
       <>
         <HeroSection />
         <FeaturesSection />
         <CTASection />
       </>
     )
   }
   ```

---

## 🚀 Процесс 6: Оптимизация производительности

### Перед деплоем проверь:

1. **Изображения оптимизированы**
   - Используй WebP формат
   - Добавляй `loading="lazy"`
   - Размер не более 200KB

2. **Импорты оптимизированы**
   ```jsx
   // ✅ Правильно - tree shaking работает
   import { Button, Card } from "@/components/ui"

   // ❌ Неправильно - импортируем всё
   import * as UI from "@/components/ui"
   ```

3. **Нет лишних ререндеров**
   - Используй `React.memo()` для тяжелых компонентов
   - Выноси статичные данные за пределы компонента

4. **Lazy loading для больших страниц**
   ```jsx
   const Dashboard = lazy(() => import("./pages/Dashboard"))
   ```

---

## 📝 Шаблоны промптов для типовых задач

### Создание лендинга:
```
Создай лендинг страницу для [название продукта/услуги] на основе следующего контента:

[вставить контент]

Структура:
1. Hero секция с заголовком и CTA
2. Секция с проблемой (Card variant="burgundy")
3. Grid из [N] преимуществ
4. [Опционально: процесс, отзывы, pricing]
5. Финальная CTA с формой

Используй компоненты из дизайн-системы согласно CLAUDE.md.
```

### Добавление секции:
```
Добавь новую секцию "[название]" после [текущая секция] в файле [имя файла].

Секция должна содержать:
- [описание контента]

Используй [конкретные компоненты] из дизайн-системы.
```

### Исправление стилей:
```
В файле [имя файла] измени стили [элемента]:
- [что изменить]

Используй только токены из дизайн-системы (см. CLAUDE.md).
```

### Создание формы:
```
Создай форму [название] с полями:
- [поле 1]: Input
- [поле 2]: Select с опциями [опции]
- [поле 3]: Textarea

Используй React Hook Form с валидацией Zod.
Стилизация согласно дизайн-системе.
```

---

## ⚠️ Частые ошибки и как их избежать

### ❌ Ошибка: Жестко закодированные цвета
```jsx
// Неправильно
<div className="bg-[#56051B] text-[#FFF]">
```
**Решение:** Используй токены
```jsx
// Правильно
<div className="bg-primary text-primary-foreground">
```

### ❌ Ошибка: Игнорирование адаптивности
```jsx
// Неправильно
<Grid cols={3}>
```
**Решение:** Добавь breakpoints
```jsx
// Правильно
<Grid cols={1} md:cols={2} lg:cols={3}>
```

### ❌ Ошибка: Создание нового компонента без необходимости
**Решение:** Проверь существующие компоненты и используй `className` для кастомизации

### ❌ Ошибка: Несоответствие именования
```jsx
// Неправильно (разные стили именования)
export function my_component() { }  // snake_case
export function Mycomponent() { }   // без PascalCase
```
**Решение:** Всегда PascalCase для компонентов, camelCase для функций

### ❌ Ошибка: Отсутствие семантических тегов
```jsx
// Неправильно
<div className="text-hero">Заголовок</div>
```
**Решение:** Используй правильные HTML теги
```jsx
// Правильно
<Text variant="hero" as="h1">Заголовок</Text>
// или
<h1 className="text-hero">Заголовок</h1>
```

---

## 🎯 Следующие шаги

После создания основной структуры:
1. Проверь контраст цветов (особенно текст на бордовом)
2. Протестируй на мобильных устройствах
3. Добавь transitions для интерактивных элементов
4. Оптимизируй изображения
5. Проверь доступность (a11y)

**Помни:** Качество важнее скорости. Лучше потратить время на правильную структуру, чем потом рефакторить.
