# Полный аудит и оптимизация проекта myVibeDesign

## Цель
Провести комплексную проверку React-проекта на ошибки, проблемы со светлой/тёмной темой и оптимизировать производительность.

---

## ЧАСТЬ 1: ПРОВЕРКА НА ОШИБКИ

### 1.1 Проверка сборки
```bash
cd /Users/sergejsafronov/Documents/myVibeDesign
npm run build
```

**Что проверить:**
- ✅ Сборка завершается без ошибок
- ✅ Нет TypeScript/JSX ошибок
- ⚠️ Размер бандла (dist/assets/*.js) - должен быть < 500KB для основного файла
- ⚠️ Warnings о больших chunk'ах

**Ожидаемые проблемы:**
- Большой размер бандла из-за recharts, lucide-react
- Неиспользуемые импорты
- Missing dependencies

### 1.2 Проверка в браузере - СВЕТЛАЯ ТЕМА

**Откройте:** http://localhost:5173

**Старая страница (DesignSystemPage):**
1. **Обзор** - проверьте консоль на ошибки
2. **Цвета** - все палитры видны и правильные цвета
3. **Типографика** - все размеры текста читаются
4. **Кнопки** - все варианты кликабельны, hover эффекты работают
5. **Формы** - ⚠️ КРИТИЧНО: проверьте, что таб не ломает страницу
   - Input, Textarea работают
   - Select открывается и выбирает значения
   - Checkbox, Switch, Radio переключаются
   - Slider и Rating работают
6. **Карточки** - все варианты отображаются корректно
7. **Обратная связь** - Badge, Progress, Alert, Skeleton анимируются
8. **Данные** - Table, Accordion, Steps работают
9. **Иконки** - Lucide иконки загружаются и отображаются
10. **Расширенные** - Dialog открывается/закрывается, Tooltip показывается

**Новая страница (NewComponentsPage) - нажмите "Новые компоненты →":**
1. **Dashboard**
   - MetricWidget показывает метрики с иконками
   - DataTable загружает данные, пагинация работает
   - SearchBar фильтрует результаты
   - Sheet с фильтрами открывается
2. **Формы**
   - React Hook Form валидация срабатывает
   - DatePicker открывает календарь
   - Combobox ищет и выбирает
   - MultiSelect позволяет выбрать несколько
   - Отправка формы показывает alert
3. **Графики**
   - LineChart, BarChart, PieChart, AreaChart отрисовываются
   - Hover на графиках показывает tooltip
   - Цвета графиков соответствуют CHART_COLORS
4. **Образование**
   - CourseCard показывает изображения (могут не загрузиться - unsplash)
   - LessonCard, ProfileCards отображаются
   - Timetable группирует по дням
   - AchievementGrid показывает достижения
5. **Навигация**
   - ContextMenu открывается по правому клику
   - TreeView раскрывается/сворачивается

**Консоль браузера:**
- ❌ Errors (красные) - НЕДОПУСТИМО
- ⚠️ Warnings (жёлтые) - записать и исправить
- 💡 Info (синие) - можно игнорировать

### 1.3 Проверка в браузере - ТЁМНАЯ ТЕМА

**Переключите Dark Mode** (кнопка в header)

**Проверьте все разделы снова:**

**Критические проблемы тёмной темы:**
- ⚠️ Текст не читается (недостаточный контраст)
- ⚠️ Белый текст на белом фоне
- ⚠️ Чёрный текст на чёрном фоне
- ⚠️ Границы карточек не видны
- ⚠️ Иконки исчезают
- ⚠️ Графики используют цвета с плохой видимостью

**Проверьте каждый компонент:**

1. **Card варианты:**
   - `variant="default"` - фон должен быть тёмным (не белым)
   - `variant="burgundy"` - бордовый фон виден в тёмной теме
   - `variant="dark"` - отличается от default

2. **Typography:**
   - Hero text виден
   - Все заголовки H1-H6 читаются
   - Body text имеет достаточный контраст
   - Muted text всё ещё читается (не слишком тёмный)

3. **Формы:**
   - Input border виден
   - Placeholder text виден
   - Focus ring виден
   - Error text читается
   - Select dropdown фон корректный

4. **Кнопки:**
   - Primary button виден и кликабелен
   - Outline button имеет видимую границу
   - Ghost button виден при hover
   - Disabled state отличим

5. **Графики:**
   - Линии графиков видны
   - Tooltip фон контрастный
   - Легенда читается
   - Оси и метки видны

6. **Образовательные компоненты:**
   - CourseCard изображения загружаются
   - Level badges (A1-C2) видны и читаются
   - Progress bar видна
   - Teacher avatar виден

### 1.4 Специфические баги для проверки

#### Баг 1: Конфликт FormField
**Проверка:**
- Перейти на старую страницу → таб "Формы"
- Страница НЕ должна исчезнуть/сломаться
- Формы должны работать

**Ожидаемое поведение:** ✅ FormGroup используется, всё работает
**Если ошибка:** Импорт FormField вместо FormGroup в DesignSystemPage.jsx

#### Баг 2: Missing images в CourseCard
**Проверка:**
- Новая страница → Образование → CourseCard
- Изображения могут не загрузиться (unsplash)

**Решение:** Заменить на локальные placeholder изображения

#### Баг 3: Recharts не отображается
**Проверка:**
- Новая страница → Графики
- Должны быть 4 графика

**Если пусто:** Проверить установку recharts: `npm list recharts`

#### Баг 4: z-index кнопок переключения
**Проверка:**
- Кнопки "Новые компоненты →" и "← Старая версия" всегда поверх контента
- z-[100] должен быть достаточным

---

## ЧАСТЬ 2: ОПТИМИЗАЦИЯ ПРОИЗВОДИТЕЛЬНОСТИ

### 2.1 Анализ размера бандла

```bash
# Собрать production build
npm run build

# Проверить размеры файлов
ls -lh dist/assets/

# Должно быть примерно:
# index-*.css: ~50KB (gzip: ~10KB)
# react-vendor-*.js: ~130KB (gzip: ~43KB)
# index-*.js: < 500KB (gzip: < 150KB) ⚠️ ВАЖНО
```

**Если index.js > 500KB:**

### 2.2 Оптимизация импортов

#### Проблема 1: Импорт всей библиотеки lucide-react

**Текущий код (ПЛОХО):**
```jsx
import * as Icons from "lucide-react"
```

**Оптимизированный (ХОРОШО):**
```jsx
import { Home, User, Settings, Calendar } from "lucide-react"
```

**Действие:** Проверьте DesignSystemPage.jsx - там уже оптимизировано ✅

#### Проблема 2: Recharts импортируется полностью

**Проверьте файлы графиков:**
- `/src/components/ui/charts/line-chart.jsx`
- `/src/components/ui/charts/bar-chart.jsx`
- `/src/components/ui/charts/pie-chart.jsx`
- `/src/components/ui/charts/area-chart.jsx`

**Должно быть:**
```jsx
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer } from "recharts"
```

**НЕ должно быть:**
```jsx
import * as Recharts from "recharts"
```

### 2.3 Code Splitting

#### Оптимизация 1: Lazy loading страниц

**Обновите App.jsx:**

```jsx
import { useState, lazy, Suspense } from "react"
import { Button, LoadingOverlay } from "@/components/ui"

const DesignSystemPage = lazy(() => import("./pages/DesignSystemPage"))
const NewComponentsPage = lazy(() => import("./pages/NewComponentsPage"))

function App() {
  const [page, setPage] = useState("old")

  if (page === "new") {
    return (
      <div>
        <div className="fixed top-4 left-4 z-[100]">
          <Button onClick={() => setPage("old")} variant="outline" size="sm">
            ← Старая версия
          </Button>
        </div>
        <Suspense fallback={<LoadingOverlay />}>
          <NewComponentsPage />
        </Suspense>
      </div>
    )
  }

  return (
    <div>
      <div className="fixed top-4 right-4 z-[100]">
        <Button onClick={() => setPage("new")} variant="default" size="sm">
          Новые компоненты →
        </Button>
      </div>
      <Suspense fallback={<LoadingOverlay />}>
        <DesignSystemPage />
      </Suspense>
    </div>
  )
}

export default App
```

#### Оптимизация 2: Lazy loading графиков

**Обновите NewComponentsPage.jsx:**

```jsx
const LineChart = lazy(() => import("@/components/ui/charts").then(m => ({ default: m.LineChart })))
const BarChart = lazy(() => import("@/components/ui/charts").then(m => ({ default: m.BarChart })))
const PieChart = lazy(() => import("@/components/ui/charts").then(m => ({ default: m.PieChart })))
const AreaChart = lazy(() => import("@/components/ui/charts").then(m => ({ default: m.AreaChart })))

// В JSX оберните в Suspense:
<Suspense fallback={<div className="h-64 flex items-center justify-center">Загрузка графика...</div>}>
  <LineChart data={chartData} lines={[...]} />
</Suspense>
```

### 2.4 Оптимизация изображений

#### Проблема: CourseCard использует внешние URL

**Замените unsplash на локальные placeholder:**

```jsx
// Вместо:
image: "https://images.unsplash.com/photo-..."

// Используйте:
image: "/images/course-placeholder.jpg"
```

**Создайте плейсхолдеры:**
```bash
mkdir -p public/images
# Добавьте несколько placeholder изображений 400x300px
```

### 2.5 Мemoизация компонентов

#### Проверьте и оптимизируйте тяжёлые компоненты:

**DataTable (если много строк):**
```jsx
import { memo } from "react"

const DataTable = memo(({ columns, data, ...props }) => {
  // ... код компонента
})
```

**MetricWidget с графиками:**
```jsx
const MetricWidget = memo(({ sparkline, ...props }) => {
  // ... код компонента
}, (prevProps, nextProps) => {
  return prevProps.value === nextProps.value &&
         prevProps.sparkline === nextProps.sparkline
})
```

### 2.6 Оптимизация CSS

#### Удалите неиспользуемый Tailwind CSS:

**Проверьте tailwind.config.js:**
```js
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}", // ✅ Правильно
  ],
  // ...
}
```

**Purge неиспользуемые стили в production:**
```bash
npm run build # Tailwind автоматически удаляет неиспользуемые классы
```

### 2.7 Tree Shaking

#### Проверьте, что ES modules используются:

**В vite.config.js добавьте:**
```js
export default defineConfig({
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          'react-vendor': ['react', 'react-dom'],
          'ui-vendor': ['lucide-react', '@radix-ui/react-dialog'],
          'charts': ['recharts'],
          'forms': ['react-hook-form', 'zod', '@hookform/resolvers'],
        }
      }
    }
  }
})
```

---

## ЧАСТЬ 3: ПРОВЕРКА ACCESSIBILITY

### 3.1 Keyboard Navigation

**Проверьте с клавиатуры (Tab, Enter, Space, Arrows):**
- ✅ Все кнопки доступны через Tab
- ✅ Enter/Space активируют кнопки
- ✅ Select открывается на Arrow Down/Up
- ✅ Dialog закрывается на Escape
- ✅ Focus visible (кольцо вокруг элемента)

### 3.2 ARIA атрибуты

**Проверьте в DevTools:**
- ✅ Buttons имеют aria-label если нет текста
- ✅ Dialogs имеют aria-labelledby, aria-describedby
- ✅ Forms имеют связанные labels (for/id)
- ✅ Inputs имеют aria-invalid при ошибке

### 3.3 Color Contrast

**Проверьте инструментом:**
- Chrome DevTools → Lighthouse → Accessibility
- Или WebAIM Contrast Checker

**Минимальные требования:**
- Обычный текст: 4.5:1
- Крупный текст (>18px): 3:1
- UI элементы: 3:1

---

## ЧАСТЬ 4: ФИНАЛЬНЫЕ ПРОВЕРКИ

### 4.1 Performance тест

```bash
npm run build
npm run preview # Запустить production preview
```

**Откройте:** http://localhost:4173

**Chrome DevTools → Lighthouse:**
- Performance: > 90
- Accessibility: > 90
- Best Practices: > 90
- SEO: > 80

### 4.2 Bundle Analyzer

**Установите:**
```bash
npm install -D rollup-plugin-visualizer
```

**В vite.config.js:**
```js
import { visualizer } from 'rollup-plugin-visualizer'

export default defineConfig({
  plugins: [
    react(),
    visualizer({ open: true })
  ]
})
```

**Соберите и проанализируйте:**
```bash
npm run build
# Откроется stats.html с визуализацией
```

### 4.3 Network тест

**Chrome DevTools → Network:**
- Throttle: Fast 3G
- Перезагрузите страницу
- Время загрузки < 3 секунды
- First Contentful Paint < 1.5 секунды

---

## ЧАСТЬ 5: ЧЕКЛИСТ ИСПРАВЛЕНИЙ

### Критические ошибки (MUST FIX)
- [ ] Нет красных ошибок в консоли
- [ ] Таб "Формы" не ломает страницу
- [ ] Dark mode: весь текст читается
- [ ] Dark mode: все границы видны
- [ ] Все кнопки кликабельны в обеих темах

### Проблемы производительности (SHOULD FIX)
- [ ] Бандл < 500KB (gzip < 150KB)
- [ ] Lazy loading страниц реализован
- [ ] Графики lazy loaded
- [ ] Lucide иконки импортируются индивидуально
- [ ] Manual chunks настроены в Vite

### Оптимизации (NICE TO HAVE)
- [ ] Мemoизация тяжёлых компонентов
- [ ] Локальные изображения вместо unsplash
- [ ] Bundle analyzer проверен
- [ ] Lighthouse score > 90

### Accessibility (IMPORTANT)
- [ ] Keyboard navigation работает
- [ ] Focus visible на всех интерактивных элементах
- [ ] Color contrast соответствует WCAG
- [ ] ARIA атрибуты на месте

---

## АВТОМАТИЧЕСКИЙ ТЕСТ СКРИПТ

Создайте файл `test-audit.sh`:

```bash
#!/bin/bash

echo "🔍 Начинаю проверку проекта myVibeDesign..."

# 1. Сборка
echo "\n📦 Проверка сборки..."
npm run build 2>&1 | tee build.log
if [ $? -eq 0 ]; then
    echo "✅ Сборка успешна"
else
    echo "❌ Сборка с ошибками"
    exit 1
fi

# 2. Размер бандла
echo "\n📊 Проверка размера бандла..."
MAIN_JS=$(find dist/assets -name "index-*.js" -exec ls -lh {} \; | awk '{print $5}')
echo "Основной JS: $MAIN_JS"

# 3. Запуск dev сервера в фоне
echo "\n🚀 Запуск dev-сервера..."
npm run dev &
DEV_PID=$!
sleep 5

# 4. Проверка доступности
echo "\n🌐 Проверка доступности..."
curl -s http://localhost:5173 > /dev/null
if [ $? -eq 0 ]; then
    echo "✅ Сервер доступен"
else
    echo "❌ Сервер недоступен"
fi

# Остановка dev сервера
kill $DEV_PID

echo "\n✅ Автоматическая проверка завершена"
echo "📋 Проверьте build.log для деталей"
```

**Запуск:**
```bash
chmod +x test-audit.sh
./test-audit.sh
```

---

## КАК ИСПОЛЬЗОВАТЬ ЭТОТ ПРОМПТ

### Вариант 1: Ручная проверка
Скопируйте весь промпт и используйте как чеклист, проходя по каждому пункту.

### Вариант 2: AI-помощник
Отправьте AI:
```
Проверь проект /Users/sergejsafronov/Documents/myVibeDesign используя FULL_AUDIT_PROMPT.md.

Выполни все 5 частей:
1. Проверка на ошибки (светлая и тёмная тема)
2. Оптимизация производительности
3. Проверка accessibility
4. Финальные проверки
5. Чеклист исправлений

Для каждой найденной проблемы предоставь:
- Описание проблемы
- Файл и строку кода
- Конкретное решение с кодом

После проверки создай отчёт в формате:
## Найденные проблемы: X
## Критические: X
## Средние: X
## Низкие: X
```

### Вариант 3: Playwright тесты
Создайте E2E тесты на основе этого чеклиста.

---

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ

После выполнения всех пунктов:

✅ **0 ошибок** в консоли браузера
✅ **Обе темы** работают идеально
✅ **Размер бандла** оптимизирован (< 500KB)
✅ **Lighthouse score** > 90
✅ **Accessibility** соответствует WCAG
✅ **Время загрузки** < 3 сек на Fast 3G

**Готовый production-ready проект!** 🚀
