# Промпт для проверки проекта на ошибки

## Задача
Провести комплексную проверку React-проекта myVibeDesign на ошибки, несоответствия и проблемы.

## Что нужно проверить

### 1. **Сборка и компиляция**
- Запустить `npm run build` и проверить на ошибки компиляции
- Проверить размер бандла (должен быть оптимальным)
- Убедиться, что нет неиспользуемых импортов и мёртвого кода

### 2. **Браузерная консоль**
- Открыть проект в браузере (http://localhost:5173)
- Проверить консоль на наличие:
  - ❌ Errors (красные ошибки)
  - ⚠️ Warnings (жёлтые предупреждения)
  - 🔍 React DevTools ошибки (например, missing keys, prop types)

### 3. **Функциональность компонентов**
Пройтись по каждому табу и проверить:

**Старая страница (DesignSystemPage.jsx):**
- ✅ Обзор - метрики отображаются
- ✅ Цвета - все палитры видны
- ✅ Типографика - все размеры текста
- ✅ Кнопки - все варианты кликабельны
- ⚠️ **Формы** - проверить, что при нажатии на таб не исчезает страница
- ✅ Карточки - все варианты (default, burgundy, dark)
- ✅ Обратная связь - Badge, Progress, Alert, Skeleton
- ✅ Данные - Avatar, Table, Accordion, Steps
- ✅ Иконки - Lucide иконки отображаются
- ✅ Расширенные - Dialog, Tooltip, FileUpload, Breadcrumb, Pagination

**Новая страница (NewComponentsPage.jsx):**
- ✅ Dashboard - MetricWidget, DataTable, SearchBar, Filters
- ✅ Формы - React Hook Form + Zod валидация
- ✅ Графики - LineChart, BarChart, PieChart, AreaChart
- ✅ Образование - CourseCard, LessonCard, ProfileCards, Timetable, Achievements
- ✅ Навигация - ContextMenu, TreeView

### 4. **Импорты и экспорты**
- Проверить, что все компоненты правильно экспортируются из `src/components/ui/index.js`
- Убедиться, что нет дублирующихся или конфликтующих экспортов
- Проверить, что все импорты в страницах корректны

### 5. **React Hook Form интеграция**
- Проверить, что старый `FormField` переименован в `FormGroup`
- Убедиться, что новый `FormField` работает только с React Hook Form
- Проверить, что форма с Zod валидацией работает корректно

### 6. **Стили и Tailwind CSS**
- Убедиться, что все кастомные цвета (burgundy, status, level, chart) доступны
- Проверить, что transition токены и easing работают
- Проверить Dark Mode переключение

### 7. **TypeScript / PropTypes**
Если используется TypeScript:
- Проверить на type errors
- Убедиться, что все пропсы типизированы

### 8. **Performance**
- Проверить время первого рендера
- Убедиться, что нет ненужных ре-рендеров
- Проверить размер бандла (должен быть < 1MB для production)

### 9. **Accessibility**
- Проверить keyboard navigation
- Убедиться, что все интерактивные элементы доступны с клавиатуры
- Проверить ARIA атрибуты

### 10. **Специфические проблемы из багрепорта**

#### Проблема 1: Не видно новых компонентов
**Симптом:** После аудита пользователь не видит новые компоненты
**Причина:** Страница DesignSystemPage.jsx не обновлена, показывает только старые компоненты
**Решение:** ✅ Создана новая страница NewComponentsPage.jsx с кнопкой переключения

#### Проблема 2: При клике на таб "Формы" всё исчезает
**Симптом:** При нажатии на таб "Формы" дизайн-система исчезает до перезагрузки страницы
**Причина:** Конфликт между старым `FormField` (простая обёртка) и новым `FormField` (React Hook Form)
**Решение:** ✅ Создан `FormGroup` для старой страницы, старый `FormField` переименован

## Команды для проверки

```bash
# 1. Установить зависимости
npm install

# 2. Проверить сборку
npm run build

# 3. Запустить dev-сервер
npm run dev

# 4. Открыть в браузере
# http://localhost:5173

# 5. Проверить ESLint (если настроен)
npm run lint

# 6. Проверить форматирование (если настроен Prettier)
npm run format
```

## Чеклист проверки

- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] Dev-сервер запускается без ошибок (`npm run dev`)
- [ ] В браузерной консоли нет красных ошибок
- [ ] Все табы на старой странице открываются без ошибок
- [ ] Таб "Формы" не ломает страницу
- [ ] Кнопка "Новые компоненты →" видна и работает
- [ ] Новая страница показывает все новые компоненты
- [ ] React Hook Form + Zod валидация работает
- [ ] Все графики (LineChart, BarChart, PieChart, AreaChart) отображаются
- [ ] Образовательные компоненты (CourseCard, LessonCard, etc.) работают
- [ ] DataTable с поиском и фильтрами функционирует
- [ ] NotificationCenter показывает уведомления
- [ ] Dark Mode переключается корректно
- [ ] ContextMenu открывается по правому клику
- [ ] TreeView раскрывается/сворачивается

## Статус исправлений

### ✅ Исправлено
1. **Новая страница создана** - NewComponentsPage.jsx с демонстрацией всех новых компонентов
2. **Роутинг добавлен** - App.jsx теперь переключает между старой и новой страницей
3. **Конфликт FormField решён** - создан FormGroup для старой страницы
4. **Импорты обновлены** - все новые компоненты экспортируются из index.js

### ⏳ Требует проверки
- Протестировать в браузере все компоненты
- Проверить консоль на наличие предупреждений
- Убедиться, что все импорты работают

### 🔮 Потенциальные проблемы
- **Размер бандла** - может быть большим из-за recharts (проверить code splitting)
- **Missing images** - CourseCard использует внешние изображения (могут не загрузиться)
- **React Hook Form deps** - проверить, что react-hook-form, zod, @hookform/resolvers установлены

## Как использовать этот промпт

Скопируйте и отправьте ИИ-помощнику:

```
Проверь проект myVibeDesign на ошибки, используя чеклист из файла AUDIT_PROMPT.md.
Пройдись по каждому пункту и сообщи о найденных проблемах.
```
